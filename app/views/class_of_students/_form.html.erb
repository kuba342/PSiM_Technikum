<%= form_with(model: class_of_student) do |form| %>
  <% if class_of_student.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(class_of_student.errors.count, "error") %> prohibited this class_of_student from being saved:</h2>

      <ul>
        <% class_of_student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :symbol, style: "display: block" %>
    <%= form.text_field :symbol %>
  </div>

  <div>
    <%= form.label :yearOfStart, style: "display: block" %>
    <%= form.number_field :yearOfStart %>
  </div>

  <div>
    <%= form.label :teacher_id, style: "display: block" %>
    <%= form.text_field :teacher_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
