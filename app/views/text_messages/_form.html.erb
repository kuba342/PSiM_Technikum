<%= form_with(model: text_message) do |form| %>
  <% if text_message.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(text_message.errors.count, "error") %> prohibited this text_message from being saved:</h2>

      <ul>
        <% text_message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_field :message %>
  </div>

  <div>
    <%= form.label :topic, style: "display: block" %>
    <%= form.text_field :topic %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
